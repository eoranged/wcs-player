{
  "name": "telegram-audio-player",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "dev": "node scripts/generate-build-info.js && next dev -p 3000",
    "build": "node scripts/generate-build-info.js && next build",
    "start": "next start -p 3000",
    "lint": "next lint",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:prod": "node scripts/generate-build-info.js && next build"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@sentry/nextjs": "^9.18.0",
    "@sentry/profiling-node": "^1.3.2",
    "next": "^15.3.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "postcss": "^8.4.27",
    "tailwindcss": "^3.3.3"
  }
}
